---
title: Playground
---

# Playground

A live pattern editor in the browser — presets, a mini-notation pattern lab, a full
JS Composer against the real API, and a single-voice tweaker. Browsers require a user
gesture before audio can play, so the first click on anything below is what creates the
`AudioContext`.

<Playground />
